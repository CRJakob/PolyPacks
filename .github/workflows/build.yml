name: Compile and Debug TypeScript on dev

on:
  push:
    branches:
      - dev

jobs:
  compile:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Debug workspace
        run: |
          echo "Working directory: $GITHUB_WORKSPACE"
          echo "----- Full tree -----"
          ls -R .

      - name: Clean old JS
        run: |
          echo "Before rm:"
          ls -l PolyPack/0.1.0/main.mod.js || echo "[not found]"
          rm -v PolyPack/0.1.0/main.mod.js || true
          echo "After rm:"
          ls -l PolyPack/0.1.0/main.mod.js || echo "[not found]"

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install TypeScript
        run: npm install typescript --no-save

      - name: Compile main.mod.ts → main.mod.js
        run: |
          npx tsc \
            --target ES2017 \
            --module commonjs \
            --rootDir . \
            --outDir . \
            PolyPack/0.1.0/main.mod.ts

      - name: Debug post-compile
        run: |
          echo "----- After compile -----"
          ls -l PolyPack/0.1.0
          echo "Contents of main.mod.js:"
          cat PolyPack/0.1.0/main.mod.js || echo "[not found]"